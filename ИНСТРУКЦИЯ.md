# Инструкция по запуску проекта React + ASP.NET Core

## Проблема решена!

Вместо использования Visual Studio для создания проекта (которое вызывало зависания), проект был создан через командную строку.

## Как запустить проект

### Шаг 1: Запустите ASP.NET Core API

Откройте PowerShell или командную строку и выполните:

```bash
cd C:\ReactAspNetCoreProject
dotnet run
```

Дождитесь сообщения о том, что сервер запущен. Обычно это:
- HTTPS: https://localhost:7241
- HTTP: http://localhost:5159

### Шаг 2: Запустите React приложение

Откройте **НОВЫЙ** терминал (не закрывая первый) и выполните:

```bash
cd C:\ReactAspNetCoreProject\clientapp
npm start
```

Через несколько секунд откроется браузер с адресом http://localhost:3000

### Шаг 3: Проверьте работу

В браузере вы должны увидеть:
- React приложение с заголовком "React + ASP.NET Core App"
- Таблицу с прогнозом погоды, полученным из API
- Кнопку "Обновить данные"

## Если возникают проблемы

### Ошибка CORS
Убедитесь, что оба сервера запущены (ASP.NET Core и React)

### Ошибка с сертификатом HTTPS
При первом запуске браузер может потребовать принять самоподписанный сертификат. Нажмите "Дополнительно" → "Перейти на сайт" (или "Advanced" → "Proceed to localhost")

### Порт занят
Если порт 7241 или 3000 занят, измените порты в:
- `Properties/launchSettings.json` (для ASP.NET)
- `clientapp/package.json` (для React, добавьте PORT=3001 в скрипт start)

## Структура проекта

```
ReactAspNetCoreProject/
├── Program.cs              # ASP.NET Core API
├── MyReactApp.Server.csproj
├── clientapp/              # React приложение
│   ├── src/
│   │   └── App.tsx         # Главный React компонент
│   └── package.json
└── README.md               # Полная документация
```

## Что дальше?

- Добавьте больше API endpoints
- Создайте контроллеры для лучшей организации кода
- Подключите базу данных
- Добавьте аутентификацию

Проект готов к разработке! 🚀

